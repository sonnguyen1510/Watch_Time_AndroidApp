<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:title="Item"
        android:id="@+id/add_worldClockmenu"
        app:showAsAction="always"
        android:icon="@drawable/ic_baseline_add_alarm_24"
        />

    <item
        android:title="Delete All"
        app:showAsAction="always"
        android:visible="false"
        android:id="@+id/world_clock_delete_all"
        />
    <item android:id="@+id/world_clock_edit"
        app:showAsAction="always"
        android:title="Edit"
        android:visible="true"
        />
    <item
        android:id="@+id/DeleteSecletedWorldClock"
        app:showAsAction="always"
        android:title="Delete"
        android:visible="false"></item>
</menu>